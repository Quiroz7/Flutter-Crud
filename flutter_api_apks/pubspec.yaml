name: flutter_api_apks
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.1 <4.0.0'

dependencies:
  camera: ^0.10.0+9
  flutter:
    sdk: flutter
  flutter_map: ^5.0.0
  geolocator: ^10.0.1
  http: ^1.1.0
  image_picker: ^1.0.4
  latlong2: ^0.9.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
 